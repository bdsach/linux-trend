curl list

```bash
curl 'https://distrowatch.com/dwres.php?resource=popularity' > ./raw/distro-list.html
```

curl distro

```bash
curl 'https://distrowatch.com/table.php?distribution=cachyos' > ./raw/cachyos.html
```